import { DefaultTheme } from "react-native-paper";

const theme = {
	...DefaultTheme,
	colors: {
		...DefaultTheme.colors,
		primary: "#B1afff",
		secondary: "#DFD8F7",
		tertiary: "#9bc4cb",
	},
};

export default theme;
